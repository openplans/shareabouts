<%= form_for Comment.new(:commentable_id => commentable.id, :commentable_type => commentable.class.to_s), 
  :url => commentable_comments_path(commentable) do |form| %>
  <%= form.hidden_field :commenter_ip %>
  <%= form.text_area :comment, :rows => 4 %>
  <%= form.submit t('map_controls.comment_feature') %>
<% end %>